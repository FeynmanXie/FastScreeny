<Window x:Class="FastScreeny.EditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Edit Screenshot" Height="800" Width="1200"
        WindowStartupLocation="CenterScreen"
        AllowsTransparency="True"
        WindowStyle="None"
        Background="Transparent">
    <Window.Resources>
        <!-- 现代化渐变背景 -->
        <LinearGradientBrush x:Key="EditorGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#0f172a" Offset="0"/>
            <GradientStop Color="#1e293b" Offset="0.5"/>
            <GradientStop Color="#020617" Offset="1"/>
        </LinearGradientBrush>
        
        <!-- 工具栏背景 -->
        <SolidColorBrush x:Key="ToolbarBackground" Color="#1e293b" Opacity="0.95"/>
        
        <!-- 现代化工具按钮样式 -->
        <Style x:Key="ToolButton" TargetType="Button">
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#475569" Offset="0"/>
                        <GradientStop Color="#334155" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="Foreground" Value="#f1f5f9"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="10"
                                Padding="{TemplateBinding Padding}">
                            <Border.Effect>
                                <DropShadowEffect Color="#000000" BlurRadius="6" ShadowDepth="2" Opacity="0.2"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                            <GradientStop Color="#64748b" Offset="0"/>
                                            <GradientStop Color="#475569" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                            <GradientStop Color="#374151" Offset="0"/>
                                            <GradientStop Color="#1f2937" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- 主要操作按钮样式 -->
        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource ToolButton}">
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#3b82f6" Offset="0"/>
                        <GradientStop Color="#1d4ed8" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background">
                        <Setter.Value>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                <GradientStop Color="#60a5fa" Offset="0"/>
                                <GradientStop Color="#2563eb" Offset="1"/>
                            </LinearGradientBrush>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <!-- 危险操作按钮样式 -->
        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource ToolButton}">
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#dc2626" Offset="0"/>
                        <GradientStop Color="#991b1b" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background">
                        <Setter.Value>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                <GradientStop Color="#ef4444" Offset="0"/>
                                <GradientStop Color="#b91c1c" Offset="1"/>
                            </LinearGradientBrush>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <!-- 现代化下拉框样式 -->
        <Style x:Key="ModernComboBox" TargetType="ComboBox">
            <Setter Property="Background" Value="#374151"/>
            <Setter Property="Foreground" Value="#f9fafb"/>
            <Setter Property="BorderBrush" Value="#4b5563"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
    </Window.Resources>
    
    <Border Background="#20000000" CornerRadius="16" Margin="8">
        <Border.Effect>
            <DropShadowEffect Color="#000000" BlurRadius="30" ShadowDepth="10" Opacity="0.5"/>
        </Border.Effect>
        
        <Grid Background="{StaticResource EditorGradient}" ClipToBounds="True">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- 自定义标题栏 -->
            <Border Grid.Row="0" Background="#20FFFFFF" CornerRadius="16,16,0,0" Padding="24,12">
                <Grid>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Ellipse Width="8" Height="8" Fill="#ef4444" Margin="0,0,6,0"/>
                        <Ellipse Width="8" Height="8" Fill="#f59e0b" Margin="0,0,6,0"/>
                        <Ellipse Width="8" Height="8" Fill="#10b981" Margin="0,0,16,0"/>
                        <TextBlock Text="💻 Screenshot Editor" Foreground="White" FontSize="16" FontWeight="Bold"/>
                    </StackPanel>
                    <Button Content="✕" HorizontalAlignment="Right" Background="Transparent" Foreground="#9ca3af" 
                            BorderThickness="0" FontSize="16" Padding="8" Click="CloseBtn_Click"/>
                </Grid>
            </Border>

            <!-- 绘制工具栏 -->
            <Border Grid.Row="1" Background="{StaticResource ToolbarBackground}" Padding="20,16">
                <StackPanel>
                    <TextBlock Text="🎨 Drawing Tools" Foreground="#cbd5e1" FontSize="14" FontWeight="Medium" Margin="0,0,0,12"/>
                    <StackPanel Orientation="Horizontal">
                        <Button x:Name="DrawingModeBtn" Content="Drawing Mode" Style="{StaticResource PrimaryButton}" Margin="0,0,16,0" Click="DrawingModeBtn_Click"/>
                        <TextBlock Text="Brush Preset" Foreground="#94a3b8" VerticalAlignment="Center" Margin="0,0,12,0"/>
                        <ComboBox x:Name="BrushPresetBox" Style="{StaticResource ModernComboBox}" Width="160" Margin="0,0,20,0"/>
                        <Button x:Name="ToolCircleBtn" Content="● Circle" Style="{StaticResource ToolButton}" Margin="0,0,12,0" Click="ToolCircleBtn_Click"/>
                        <Button x:Name="ToolRectBtn" Content="■ Rectangle" Style="{StaticResource ToolButton}" Margin="0,0,12,0" Click="ToolRectBtn_Click"/>
                        <Button x:Name="ToolArrowBtn" Content="→ Arrow" Style="{StaticResource ToolButton}" Margin="0,0,20,0" Click="ToolArrowBtn_Click"/>
                        <Button x:Name="ClearBtn" Content="🗑 Clear Annotations" Style="{StaticResource DangerButton}" Click="ClearBtn_Click"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- 边框设置栏 -->
            <Border Grid.Row="2" Background="#15FFFFFF" Padding="20,14">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <CheckBox x:Name="EnableBorderCheck" Content="🖼️ Enable Border" Foreground="#e2e8f0" VerticalAlignment="Center" Margin="0,0,24,0"/>
                    <TextBlock Text="Thickness" Foreground="#94a3b8" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBox x:Name="BorderThicknessBox" Width="60" Background="#374151" Foreground="#f9fafb" BorderBrush="#4b5563" Padding="8,6" Margin="0,0,20,0"/>
                    <TextBlock Text="Preset" Foreground="#94a3b8" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <ComboBox x:Name="BorderPresetBox" Style="{StaticResource ModernComboBox}" Width="140" Margin="0,0,20,0"/>
                    <TextBox x:Name="BorderStartColorBox" Width="80" Background="#374151" Foreground="#f9fafb" BorderBrush="#4b5563" Padding="8,6" Margin="0,0,8,0"/>
                    <TextBox x:Name="BorderEndColorBox" Width="80" Background="#374151" Foreground="#f9fafb" BorderBrush="#4b5563" Padding="8,6"/>
                </StackPanel>
            </Border>

            <!-- 编辑区域 -->
            <ScrollViewer Grid.Row="3" Background="#10FFFFFF" Margin="8,0,8,0">
                <Grid x:Name="EditRoot" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="20">
                    <Border Background="#05FFFFFF" CornerRadius="8" Padding="8">
                        <Grid>
                            <Image x:Name="PreviewImage" Stretch="None" Visibility="Collapsed"/>
                            <Image x:Name="BaseImage" Stretch="None"/>
                            <Canvas x:Name="OverlayCanvas" Background="Transparent" 
                                    MouseLeftButtonDown="OverlayCanvas_MouseLeftButtonDown" 
                                    MouseMove="OverlayCanvas_MouseMove" 
                                    MouseLeftButtonUp="OverlayCanvas_MouseLeftButtonUp"/>
                        </Grid>
                    </Border>
                </Grid>
            </ScrollViewer>

            <!-- 底部操作栏 -->
            <Border Grid.Row="4" Background="#15FFFFFF" CornerRadius="0,0,16,16" Padding="24,16">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="SaveBtn" Content="💾 Save" Style="{StaticResource PrimaryButton}" MinWidth="100" Margin="0,0,12,0" Click="SaveBtn_Click"/>
                    <Button x:Name="CloseBtn" Content="Close" Style="{StaticResource ToolButton}" MinWidth="100" Click="CloseBtn_Click"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
    
</Window>


